# Refresh-токен из веб-версии БКС Мир Инвестиций
BCS_REFRESH_TOKEN=your_refresh_token_here

# Тип токена: "trade-api-read" (только чтение) или "trade-api-write" (торговля + чтение)
BCS_CLIENT_ID=trade-api-read

# Базовый URL API (не менять без необходимости)
BCS_BASE_URL=https://be.broker.ru

# Транспорт MCP: "sse" (HTTP-сервер) или "stdio" (стандартные потоки)
MCP_TRANSPORT=sse

# Порт для SSE-транспорта
MCP_PORT=7491
